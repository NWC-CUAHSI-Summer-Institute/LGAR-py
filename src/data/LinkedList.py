import logging

log = logging.getLogger("data.LinkedList")


# class Node:
#     def __init__(self,front_num):
#         self.front_num = front_num
#         self.next = None # the pointer initially points to nothing
#
#     def traverse(self):
#         node = self # start from the head node
#         while node is not None:
#             log.debug(node) # access the node value
#             node = node.next # move on to the next node
#
#
# class LinkedList:
#     def __init__(self):
#         self.head = None

